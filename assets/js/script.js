var a = function ($, window, document, undefined) {
	$(function () {
		var ADAM = window.ADAM;

		var $window   = $(window),
			$document = $(document),
			$html     = $(document.documentElement),
			$body     = $(document.body),
			$head     = $('head'),
			$surface  = $body,
			$content  = $('.content', $surface);
	})
}($, window, document)

